<%
const lang = locals.lang || "en";

const meta = {
    description: dictionary.getText("Discloud - Internal Server Error"),
    keywords: dictionary.getText("discloud, internal server error"),
    title: dictionary.getText("Discloud | Internal Server Error")
};

const option = {
    yandexMetrika: false
};
%>

<!doctype html>
<html lang="<%= lang %>">
<head>
    <% include head.ejs %>
    <link rel="stylesheet" type="text/css" href="/css/index.bundle.css"/>
</head>
<body>
    <% include nav.ejs %>
    <div class="container e500">
        <div class="row">
            <div class="four columns">
                <img src="/img/bunny-500.png" alt="Error bunny"/>
            </div>
            <div class="four columns">
                <h1><%= dictionary.getText("Internal Server Error") %></h1>
                <h4><%= dictionary.getText("Error 500") %></h4>
                <p><%= dictionary.getText("Sorry, unexpected error") %></p>
                <a class="button" href="/"><%= dictionary.getText("Go back") %></a>
            </div>
            <div class="four columns">
            </div>
        </div>
    </div>
<script src="/js/index.bundle.js"></script>
<% include script.ejs %>
</body>
</html>